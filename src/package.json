{
  "name": "src",
  "version": "1.0.0",
  "scripts": {
    "build": "npm-run-all build:*",
    "build:get-product-by-id": "cd GetProductById && npm run build",
    "build:get-products-list": "cd GetProductsList && npm run build"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
